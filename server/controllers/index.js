'use strict';

const menu = require( './menu' );
const menuItem = require( './menu-item' );
const relations = require( './relations' );

module.exports = {
  menu,
  'menu-item': menuItem,
  relations,
};
